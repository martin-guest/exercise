{{ ansible_managed | comment }}
*.*  action(type="omfwd" target="{{ remote_syslog_target }}" port="{{ remote_syslog_port }}" protocol="{{ remote_syslog_protocol }}"
            action.resumeRetryCount="100"
            queue.type="linkedList" queue.size="10000")
